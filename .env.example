# Resend API key (required for waitlist + inbound forwarding)
RESEND_API_KEY=your-resend-api-key

# Default inbox that receives waitlist submissions
WAITLIST_FORWARD_TO=andrew@protobuf.ai

# Optional: enables AI-personalized waitlist confirmations
OPENAI_API_KEY=sk-your-openai-key

# Optional: comma-separated list of inboxes that should receive forwarded inbound mail
RESEND_INBOUND_FORWARD_TO=andrew@protobuf.ai
